# voice-controlled-design-tool
A browser-based design tool where operations that normally need two hands, panning, multi-select, layer reordering, can be performed by voice. A fine-tuned DistilBERT classifier maps spoken phrases to 30 canvas commands.
